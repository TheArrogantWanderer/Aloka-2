import React from 'react'
import ReactDOM from 'react-dom/client'
import AlokaApp from '../aloka.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AlokaApp />
  </React.StrictMode>,
)
